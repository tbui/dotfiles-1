#
# ~/.bash_profile
#

# This file is loaded once during login.
# Configure 'NeoVim / NVIM'
export EDITOR=nvim	# Set nvim as the default editor.
alias vi="nvim"		# Alias vim to nvim.
alias vim="nvim"

[[ -f ~/.bashrc ]] && . ~/.bashrc
